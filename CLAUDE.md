# 프로젝트 지침 (CLAUDE.md)

## 프로젝트 개요
AI 기반 은행 검사부문 선정(검사기획) 운영체계의 **기준 저장소**.
핵심 원칙: Risk-Based · Evidence-Based · Explainable AI · Human-in-the-loop · Continuous Learning.
**AI는 검사부문을 결정하지 않고 근거와 함께 추천만 하며, 최종 결정은 검사협의회가 수행한다.**
구조·문서 개요는 `README.md` 참조.

## 작업 대상 검증 규율 (저장소·브랜치 불일치 시 반드시 사전 확인)
지시된 저장소·브랜치·작업 디렉터리가 실제 환경과 어긋나면, **임의 판단(우회 포함)으로 진행하지 않고 반드시 먼저 확인한다.**

- **파일 생성·커밋 전 대상 검증**: `git remote -v` / 작업 디렉터리 / `list_repos`로 실제 대상 저장소를 먼저 식별한다.
- **불일치 시 사전 확인**: `AskUserQuestion`으로 사용자에게 확인한 뒤 진행한다. "기존 파일을 안 깨뜨린다"는 안전성과 "올바른 대상인가"는 별개다 — 하위 디렉터리 생성 등 **우회로 임의 진행 금지**.
- **멈춤 신호(하나라도 해당하면 중단·확인)**:
  1. 지시된 저장소 이름 ≠ 실제 작업 디렉터리 / 세션 스코프
  2. 지시된 브랜치가 현재 저장소에 없거나 다른 저장소를 가리킴
  3. "새 저장소를 만든다"는데 무관한 기존 프로젝트가 디렉터리에 있음
- **배경**: 2026-06-30, 이 framework 대상 작업이 `Daily-Morning-brief` 하위 디렉터리로 잘못 생성된 사례 재발방지.
